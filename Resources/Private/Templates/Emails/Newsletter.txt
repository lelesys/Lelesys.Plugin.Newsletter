<f:for each="{contentNode}" as="node">
	<f:for each="{node.properties}" key="nodeType" as="property">
		<f:switch expression="{nodeType}">
			<f:case value="text"><f:format.stripTags>{property-> f:format.raw()}</f:format.stripTags></f:case>
			<f:case value="image">{f:uri.resource(resource: property.resource)}</f:case>
		</f:switch>
	</f:for>
</f:for>
<f:if condition="{recipientId}">
	<f:then>
		Please click the link to unSubscribe : {baseUrl}?recipientId={recipientId}&code={code}
	</f:then>
</f:if>